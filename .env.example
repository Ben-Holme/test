VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional: used by the bank statement import feature (/importera)
# Can also be entered directly in the UI and is stored in localStorage
VITE_ANTHROPIC_API_KEY=
